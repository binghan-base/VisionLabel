# 第20步：统一标注状态管理

本步目标：把文件列表里的状态从分散的字符串判断，统一成与标注类型无关的状态模型。

## 新增文件

- `visionlabel/models/__init__.py`
- `visionlabel/models/annotation_status.py`

## 五种状态

- `TODO`：没有 JSON，尚未确认完成。
- `DONE`：JSON 存在，且至少有一个标注对象。
- `EMPTY`：JSON 存在，但标注列表为空，表示人工确认无目标。
- `DIRTY`：当前内存内容发生修改但尚未保存。
- `ERROR`：JSON 存在但读取失败。

## 为什么与未来标注类型兼容

状态计算只依赖：标注对象数量、JSON 是否存在、是否有未保存修改、是否读取错误。它不判断 `rectangle`、`polygon`、`point` 等具体类型，因此以后新增标注类型时不需要改状态模块。

## 本步验证

1. 打开无 JSON 图片：文件列表显示 `○ 未标注`。
2. 新画一个框但不保存：显示 `● 未保存`。
3. 保存后：显示 `✓ 已标注`。
4. 空图片按 `Ctrl+S` 或“保存空标签”：显示 `✓ 空标注`。
5. 删除一张已标注图片的最后一个框：保存前显示 `● 未保存`；普通保存后删除旧 JSON，并显示 `○ 未标注`。
6. 打开损坏 JSON：显示 `⚠ 标注损坏`，按当前恢复流程处理。

## 新知识：Enum

`AnnotationStatus` 使用 Python 的 `Enum` 表示固定状态。相比到处写 `"done"`、`"empty"`，枚举可以减少拼写错误，也方便 IDE 自动提示和后续维护。
